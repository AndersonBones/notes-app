{
    "version":2,
    "builds":[
        {
            "src":"dist/server.js",
            "use":"@vercel/node",
            "conig":{"includeFiles":["dist/**"]}
        }
    ],

    "routes":[
        {
            "src":"/(.*)",
            "dest":"dist/server.js"
        }

    ]
}